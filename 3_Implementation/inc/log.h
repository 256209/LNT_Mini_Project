/**
 * @brief Function to add the date the record was logged to the system
 * 
 * @param fptr 
 * @param date 
 * @return int 
 */
int log_patient(FILE *fptr, int date[]);
/**
 * @brief Function to open the file the record is stored in 
 * 
 */
void openfile();
