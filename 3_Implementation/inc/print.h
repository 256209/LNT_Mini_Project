/**
 * @brief function to print the record from the file pointer passed as argument
 * 
 * @param fptr 
 */
void print(FILE *fptr);
/**
 * @brief function to toggle menu
 * 
 */
void menu();
